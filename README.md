
## Installation

Create an .env file in the fe and be folders.

In the root project, run this command.

```bash
  docker compose up --build
```

FE URL:
```bash
  http://localhost:3000
```

BE URL:
```bash
  http://localhost:5000
```
    
